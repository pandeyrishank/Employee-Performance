# Application name (optional, used in logging)
spring.application.name=employee_performance

# MySQL DB connection
spring.datasource.url=jdbc:mysql://localhost:3306/employee_database?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Invader@2506
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (DDL auto generation - recommended for dev only)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
